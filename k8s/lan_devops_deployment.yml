apiVersion: apps/v1
kind: Deployment
metadata:
  name: lan-devops
  labels:
    app: lan-devops
spec:
  replicas: 10
  selector:
    matchLabels:
      app: lan-devops
  template:
    metadata:
      name: lan-devops
      labels:
        app: lan-devops
    spec:
      containers:
        - name: lan-devops-container
          image: gnomeshift/lan_devops:v0.9.0
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "200Mi"
              cpu: "20m"
            requests:
              memory: "100Mi"
              cpu: "10m"